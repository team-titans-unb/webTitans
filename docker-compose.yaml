services:
  auth:
    image: auth
    build:
      context: .
      target: deps
    ports:
      - "5000:8080"
    environment:
      - WATCHPACK_POLLING=true
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - /app/.next
    command: npm run dev

volumes:
  node_modules:
